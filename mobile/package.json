{
  "name": "mobile",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "dev:ios": "cap sync && cap open ios",
    "dev:android": "cap sync && cap open android",
    "open:ios": "cap open ios",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
